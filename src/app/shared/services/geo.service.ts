import {GeoData} from '../dto/geoData';

export class GeoService {
  private const _earthRadius: number = 6371;
  isInRange(currentPosition: GeoData, positionToCheck: GeoData): bool {
      

  }


}
